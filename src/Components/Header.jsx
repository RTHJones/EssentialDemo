import React from 'react';

// In the Header Component no state is required and so a class is not used.
const Header = () => {
    return (
        <div>
            <h1>Robin's Profile Page</h1>
        </div>
    );
};

export default Header;